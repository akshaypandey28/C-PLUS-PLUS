Join : the thread that calls join() (ex => the main thread) is the one that waits
Note : If we create multiple threads at the same time it doesn't gurantee which one will start first

#include <chrono> //used for time measurement, durations, and clocks
using namespace std::chrono; //Bring everything inside std::chrono into the current scope.

WAYS TO CREATE THREADS IN C++11
1. Function Pointers
2. Lambda Functions
3. Functors
4. Member Functions
5. Static Member functions

